# ============================================================
# INSPECT SOURCE DATA FOR MONTHLY FACTOR CREATION
# ============================================================

source_dfs = {
    "monthly_hiring": monthly_hiring,
    "monthly_headcount": monthly_headcount,
    "resignation_data": resignation_data,
    "monthly_tenure": monthly_tenure,
    "attendance_metrics": attendance_metrics,
    "ot_features": ot_features,
}

for name, data in source_dfs.items():

    print("\n" + "=" * 80)
    print(name.upper())
    print("=" * 80)

    print("Shape:", data.shape)
    print("Columns:")
    print(data.columns.tolist())

    print("\nData types:")
    print(data.dtypes)

    print("\nSample:")
    display(data.head(5))
