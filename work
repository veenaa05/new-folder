# ============================================================
# CHECK FINAL MONTHLY MODEL DATA
# ============================================================

print("=" * 70)
print("FINAL MONTHLY MODEL DATA CHECK")
print("=" * 70)

print("\nShape:", model_df.shape)

print("\nColumns:")
for col in model_df.columns:
    print(" -", col)

print("\nDate range:")
print(model_df["Month"].min(), "to", model_df["Month"].max())

print("\nNon-missing values:")
print(model_df.notna().sum())

print("\nMissing values:")
print(model_df.isna().sum())

print("\nLast 12 months:")
display(model_df.tail(12))
